const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
  theme: {
    extend: {
      fontFamily: {
        poly: ["var(--font-polysans)"],
      },
    },
  },

};

export default config;
